using UnityEngine;

/// <summary>
/// Shows a dropdown for selecting an EventKey (string) from EventKeyRegistry.
/// Layout is intended to match other key pickers (dropdown + explorer button).
/// </summary>
public sealed class EventKeyDropdownAttribute : PropertyAttribute
{
}
