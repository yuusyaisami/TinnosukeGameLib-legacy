using UnityEngine;

/// <summary>
/// Shows a dropdown for selecting an ActionBlock key (string) from ActionBlockKeys.
/// Layout is intended to match other key pickers (dropdown).
/// </summary>
public sealed class ActionBlockKeyDropdownAttribute : PropertyAttribute
{
}
