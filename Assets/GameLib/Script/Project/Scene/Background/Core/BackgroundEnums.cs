#nullable enable
namespace Game.Background
{
    public enum BackgroundSpace
    {
        World = 0,
        UI = 1,
    }

    public enum BackgroundMode
    {
        Infinite = 0,
        Fixed = 1,
    }

    public enum BackgroundSpawnPivot
    {
        TileCenter = 0,
        TileOrigin = 1,
    }
}
